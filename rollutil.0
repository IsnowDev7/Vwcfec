-- Script Path: game:GetService("ReplicatedStorage").Framework.Features.Rolling.RollUtil
-- Took 0.02s to decompile.

local v1 = game:GetService("ReplicatedStorage")
local v_u_2 = require(v1.Framework.Features.Inventory.EntryRegistry)
require(v1.Framework.Features.Inventory.EntryTypes)
local v_u_3 = require(v1.Framework.Features.Inventory.Kinds.Unit.Mutations)
local v4 = {}
local v_u_5 = Random.new()
function v4.RollUnit(p6) -- name: RollUnit
    -- upvalues: (copy) v_u_2, (copy) v_u_3, (copy) v_u_5
    local v7 = 0
    local v8 = {}
    for v9, v10 in pairs(v_u_2.entriesOfKind("Unit")) do
        if v10.chance then
            local v11
            for v12, v13 in pairs(v_u_3) do
                local v14 = v10.chance() * v13.chance / (p6 or 1)
                if v14 >= 1 then
                    local v15 = 1 / v14
                    v11 = v7 + v15
                    table.insert(v8, {
                        ["unit"] = v9,
                        ["chance"] = v15,
                        ["mutation"] = v12
                    })
                    v7 = v11
                end
            end
            local v16 = v10.chance() / (p6 or 1)
            if v16 >= 1 then
                local v17 = 1 / v16
                v11 = v7 + v17
                table.insert(v8, {
                    ["unit"] = nil,
                    ["chance"] = nil,
                    ["mutation"] = nil,
                    ["unit"] = v9,
                    ["chance"] = v17
                })
                v7 = v11
            end
        end
    end
    local v18 = v_u_5:NextNumber(0, v7)
    local v19 = 0
    local v20 = nil
    local v21 = nil
    for _, v22 in pairs(v8) do
        v19 = v19 + v22.chance
        if v18 <= v19 then
            v20 = v22.unit
            v21 = v22.mutation
            break
        end
    end
    return v20, v21
end
return v4
